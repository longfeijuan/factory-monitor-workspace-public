# 生产接入清单

本清单用于把当前脱敏工作台接到现有或重建的生产录像能力。GatePersonAudit 源码可用于复用，但不是前置条件；任何一步失败都应显示“未知/证据不足”，不能返回错误的正常结论。

## 1. 基线核对

- 若使用原源码，核实版本为 `0.21.0`，不可变发布为 `release-5affc732731c3dcc`；若重建，则单独记录实现版本和验收证据；
- 运行现有测试和发布清单校验，不修改正在执行的 00:00、08:30 任务；
- 确认私密连接配置和 Keychain 继续留在原项目，不复制到本工作台。

## 2. 只读接口

工作台只需要以下受控能力：

- 按逻辑摄像头 ID 查询目标时段录像覆盖；
- 返回时间映射、OSD 校准、解码完整性和证据缺口；
- 请求代表帧或受限短片；
- 请求 YOLO26n、变化排序和 ByteTrack 的候选结果；
- 返回受控证据编号，不把录像路径或连接信息暴露给浏览器。

当前工作台提供：

- `GET /api/gate/status`：只返回安全的连接状态和能力开关；
- `POST /api/gate/coverage`：输入脱敏逻辑摄像头 ID 与最长24小时窗口；
- 上游连接器：`GATE_PERSON_AUDIT_BASE_URL`，可选令牌由 `GATE_PERSON_AUDIT_TOKEN` 提供；
- 上游需实现 `GET /health` 与 `POST /v1/coverage`，连接失败统一返回 `unknown`。

仓库中的 `connector/gate_nvr_service.py` 已实现上述契约：使用海康 Digest + ISAPI 只读搜索。macOS 一键安装器会把使用者首次输入的只读凭据保存到系统钥匙串；其他系统只在当前进程内存中保留。服务只绑定本机回环地址，并会对回放URI做不可逆摘要后才返回证据编号。

## 3. Luna 编排契约

Luna 只负责任务编排和解释候选：建立事件、选择路线、逐路检查覆盖、生成待复核列表、等待人工结论、导出事件单。它不得推断人员身份、责任、意图、考勤或处分结论。

## 4. 验收门槛

- 选择一个区域、一类丢失事件进行试点；
- 人工标注至少覆盖“有线索、无关、录像缺失、时钟不可信、无法判断”；
- 统计候选召回、误报、证据覆盖与处理耗时；
- 验证幂等、失败关闭、七天像素清理和回滚；
- 具名主管、区域范围、有效期和只读权限全部获批后，才允许真实画面进入流程。
