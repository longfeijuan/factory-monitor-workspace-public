# 工厂监控问答项目规则

本仓库的目标是让 Codex 在任何获批电脑上，按一致口径执行只读监控查询。真实录像、截图、密码、私密连接配置和人员参考图不进入 Git。

## 每次监控查询前

1. 先读取 `camera-data/current/SOURCE.json`，并运行 `python3 scripts/sync_camera_catalog.py --check`，确认仓库内置脱敏资料包及摄像头目录一致。
2. 只有被指定为“资料同步人”且有权访问钉钉“黄伟工作群”时，才运行 `python3 scripts/fetch_latest_camera_package.py --apply` 拉取新包，随后运行 `python3 scripts/sync_camera_catalog.py`、自检、提交并推送 GitHub。普通使用者不需要加入黄伟工作群，直接使用 GitHub 中已审核的内置版本。
3. 无法访问黄伟工作群时，不得绕过群权限，也不应阻塞普通问答；结果中写明 `SOURCE.json` 记录的文件名和发布时间，并称为“仓库内置版本”，不得声称它是群内最新版本。若业务必须核对群内最新发布，转交资料同步人处理。
4. Windows 普通使用者从 Windows 凭据管理器读取4台本机 NVR 只读连接项；缺少时运行 `SETUP-NVR-CREDENTIALS.cmd` 安全录入。不得要求普通使用者加入黄伟工作群或使用 `--import-from-dingtalk`；该参数只供获批资料同步人使用。凭据不得进入 Git、项目文件、报告或聊天记录。
5. 将“今天、昨天、白班、夜班”等转换为 `Asia/Shanghai` 的绝对起止时间，并在结果中写明。
6. 根据问题使用 `.agents/skills/` 中最贴合区域的技能；先完整读取其 `SKILL.md` 和该任务要求的 reference。

## 统一证据边界

- 只读访问 NVR；不得修改配置、删除录像、控制镜头或扩大用户指定时段。
- 以监控画面角标时间为准。请求时间、文件名和播放器进度只能辅助定位。
- 回放失败、无录像、无有效帧、遮挡和无法确认统一记为 `unknown/待核实`，不得当作停机、无人或事件没有发生。
- 不做人脸识别，不根据监控自动判断身份、考勤、责任、意图、授权状态或处分结论。
- 原始画面和运行结果只写入已被忽略的本地目录；报告只引用必要的本机绝对证据路径。
- 生成或修改任何真实通知、部署、NVR 配置前，另行确认授权；普通查询不包含这些写操作。

## 班次默认口径

- 白班整班：08:00–20:00；休息：12:00–13:30、17:30–18:00；有效时段共10小时。
- 夜班整班：20:00–次日08:00；休息：00:00–02:00；有效时段共10小时。
- 开机率默认按有效生产时段计算，同时保留完整12小时原始率。未知点单列，不并入停机。
- 尾2按其技能中的固定日班时间执行；用户当次明确给出不同口径时，以用户要求为准。

## 58号门跨电脑一致性

- 任何58号门人员进出问题必须使用 `.agents/skills/audit-gate58-people-crossing` 的v2唯一流程；不得根据旧对话、旧输出或聊天中临时脚本直接回答。
- 一个用户提示词内完成初审、待复核扩窗和质量闸门；不得先返回中间人数再要求用户追加“继续复核”。
- 同机同输入复跑必须完全一致；跨电脑进入、外出、合计各自偏差不超过2人次才算达成目标。

## 一楼电脑锣开机率

- 一楼电脑锣六台机的开机率、运行率、绿灯和停机时段必须使用 `.agents/skills/cnc-floor1-runtime-audit` 及 `config/cnc-floor1-runtime-v2.json`；不得误用人员在岗技能。
- 正式机台灯源固定为 `nvr-main-02` 通道49/轨道4901。通道30只作为人员在岗辅助视角，不得用于六台灯位计算。
- `2026-08-28` 起的当前画面使用1280×720的v2固定灯位。画面移动或尺寸/布局变化时必须质量闸门失败并建立新规则版本，不得把未命中灯位解释成0%。

## 全项目跨电脑一致性

- 任何会输出人数、次数、开机率、运行率或持续时长的监控查询，都必须先锁定仓库资料包、Git提交、绝对时段、区域规则和业务参数，生成项目统一 `query_id`。区域技能已有专用准备脚本时由该脚本完成；否则先运行 `scripts/monitor_query_context.py`。
- 每次必须使用全新空输出目录，禁止复用以前的帧、结果、覆盖表或对话数字；禁止在运行目录临时写脚本修数。一个用户提示词内完成固定重试和质量闸门，只发送一次最终答案。
- 区域技能有专用结果契约时优先使用；其他数值结果用 `scripts/monitor_result_contract.py` 生成 `result-envelope.json`。两台电脑用 `scripts/compare_monitor_results.py` 验收：计数默认偏差≤2、比率默认偏差≤1.0个百分点、时长默认偏差≤5分钟；区域契约更严格时从严。
- `query_id` 不同或任一质量闸门未通过时，不得比较数字，也不得把结果称为“跨电脑已达标”。同机同输入复跑必须 `identical=true`。

## 项目验证

Windows 一键安装使用 `INSTALL-WINDOWS.cmd`；项目内统一验证入口为 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify-windows.ps1`。Windows 已存在 `.venv\Scripts\python.exe` 时，监控脚本优先使用该解释器；不得因系统没有 `python3.exe` 而绕过质量门。

改动后至少运行：

```bash
python3 scripts/monitor_self_check.py
python3 scripts/sync_camera_catalog.py --check
pnpm run test:connector
pnpm test
```
